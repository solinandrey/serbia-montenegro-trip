# Белград → Черногория, сентябрь 2026

План поездки на троих: неделя с небольшим в Белграде, поезд до Бара, две недели по Черногории на машине.
Первая неделя в Черногории — свободная, вторая — рабочая.

Страница показывает по одному отрезку за раз, переключатель под шапкой. Вид держится в адресе,
так что ссылкой можно кидаться: `…/#sever` откроет север, `…/#voz` — поезд, `…/#check` — сборы.
Если адрес без хеша, страница сама открывает отрезок по сегодняшней дате.

## Что где лежит

| Файл | Что внутри | Кто правит |
|---|---|---|
| `plan.json` | Чеклист, пункты плана по отрезкам, заметки | все, через коммиты |
| `index.html` | Страница: вёрстка, профиль высот, справочные блоки | тот, кто трогает код |

Справочные факты (цены на поезд, правила регистрации, чего не делать с арендой) зашиты в `index.html`
в массиве `SEGMENTS`. Это то, что не меняется от нашего настроения — только при проверке актуальности.

Удалили пункт по ошибке — под шапкой появится полоса «Убрано… Вернуть», она живёт 25 секунд
и кладёт пункт обратно на то же место.

## Как открыть

**GitHub Pages** — Settings → Pages → Deploy from a branch → `main` / root.
Через минуту страница живёт на `https://solinandrey.github.io/serbia-montenegro-trip/`.

**Локально** — открыть `index.html` двойным кликом не выйдет: браузер запретит читать `plan.json`
с `file://`. Нужен любой локальный сервер:

```bash
python3 -m http.server 8000
# http://localhost:8000
```

## Под рукой: брони, телефоны, адреса

Вид «Ссылки» рядом с дорогой. Туда складываются номера броней, адреса, телефоны хозяев
и ссылки на жильё — всё, что иначе будет валяться в переписке у каждого своё. Записи
сгруппированы по отрезкам, общее идёт первым.

Добавить — вписать название или сразу ссылку в поле нужной группы. Дальше «Править»:
открывается форма из шести строк, лишние оставляйте пустыми, они не покажутся.

```
Название: Квартира в Белграде
Ссылка: https://…
Телефон: +381 …
Адрес: …
Бронь: номер брони
Заметка: хозяин, время заезда, код от подъезда
```

Телефон становится ссылкой — с телефона звонок в один тап. Ссылки в пунктах плана
и заметках тоже кликабельные: кинули ссылку на точку в карте — она откроется, а не будет
лежать мёртвым текстом.

## Общий план

Самое быстрое — открыть ссылку вида `…/#key=ПАРОЛЬ`: страница подключится сама, а пароль
тут же исчезнет из адресной строки, чтобы не уехать в скриншот. Можно сразу с видом:
`…/#sever&key=ПАРОЛЬ` откроет север и подключит план.

Или руками: нажмите «Подключить общий план» и введите общий пароль (он один на троих,
спросите у Андрея). Дальше всё работает само: ваши пункты, галочки и заметки видят остальные,
чужие правки появляются раз в 20 секунд и при возврате во вкладку. Пароль запоминается
в браузере, вводить каждый раз не нужно.

Правки не затирают друг друга: если двое пишут одновременно, сервер возвращает свежую версию,
а страница сливает её с вашей. Удалённый пункт можно вернуть кнопкой «Вернуть».

Без пароля страница показывает план из `plan.json` и хранит правки только в вашем браузере —
статус в шапке всегда честно пишет, в каком вы режиме.

## Как вносить правки

Два пути, оба рабочие.

**Через гит (основной).** Правите `plan.json` руками, коммитите, пушите. Страница подхватывает
изменения при следующей загрузке. Конфликты решает гит, ничьи правки не теряются.

```bash
git pull
$EDITOR plan.json
git commit -am "Жабляк: три ночи вместо двух"
git push
```

**Через страницу (быстрый).** Добавляете пункты и заметки прямо в браузере, потом жмёте
«Скачать plan.json», кладёте файл поверх старого, коммитите. Удобно, когда сидишь в кафе
и хочешь дописать мысль, но правки других при этом можно затереть — сначала `git pull`.

## Структура plan.json

```jsonc
{
  "version": 1,
  "checklist": [ { "id": "c01", "text": "…", "done": false } ],
  "segments": {
    "beograd": [ { "id": "b01", "text": "…" } ],  // Белград
    "voz":     [ … ],                             // поезд Београд Центар → Бар
    "sever":   [ … ],                             // север Черногории, свободная неделя
    "more":    [ … ]                              // побережье, рабочая неделя
  },
  "notes": [ { "id": "n01", "segment": "sever", "author": "Андрей", "text": "…", "ts": 1757000000000 } ],
  "links": [ { "id": "l01", "segment": "beograd", "title": "Квартира в Белграде",
               "url": "https://…", "phone": "+381 …", "address": "…",
               "ref": "номер брони", "note": "хозяин, заезд, код подъезда" } ]
}
```

В `links` обязательны только `id` и `title`. `segment` — `beograd` / `voz` / `sever` / `more`
либо отсутствует, тогда запись попадёт в группу «Общее».

`id` — любая уникальная строка. `ts` — миллисекунды, `Date.now()`. Ключи в `segments` менять нельзя,
они привязаны к отрезкам в `index.html`.

## Что проверить перед выездом

Всё, что помечено датами и ценами, устаревает. Особенно:

- **Расписание поезда.** Дневной сезонный «Тара» ходит по 13 сентября, летний состав ночного «Ловчен» —
  по ночь с 13 на 14-е. Проверять на `srbijavoz.rs`, международные билеты онлайн не продаются.
- **Лимит безвизовых дней** — свой для каждого паспорта, у Сербии и Черногории счётчики разные.
- **Цены на аренду машины** — сентябрьские падают после 15-го, но зелёная карта на Албанию и Боснию
  оформляется только при бронировании.
